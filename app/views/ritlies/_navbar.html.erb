<nav class="navbar navbar-default" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <a class="navbar-brand" href="/">Ritly</a>
  </div>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <form class="navbar-form navbar-left" role="search" action="/go" method="post">
      <div class="form-group">
        <input type="text" class="form-control" placeholder="Paste URL" name="ritly[link]">
      </div>
      <%= token_tag form_authenticity_token %>
      <button type="submit" class="btn btn-default btn-color">Ritylize</button>
    </form>

    <ul class="nav navbar-nav navbar-right">
      <li><a href="#" class="visits">Visits: <%= @ritly.visits if @yes %></a></li>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Most Visited <b class="caret"></b></a>
        <ul class="dropdown-menu">
          <% @ritlies.each do |r| %>
            <li><%= link_to shorten(r.link), action: :show, random_str: r.random_string %></li>
          <% end %>
    
          <li class="divider"></li>
          <li><a href="/go">View All Ritlynks</a></li>
        </ul>
      </li>
    </ul>
  </div><!-- /.navbar-collapse -->
</nav>